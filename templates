<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Oficina de Motos</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>

<nav class="navbar navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="/">🏍️ Oficina de Motos</a>
        <div>
            <a class="btn btn-outline-light btn-sm" href="/">Em serviço</a>
            <a class="btn btn-outline-light btn-sm" href="/historico-hoje">Finalizadas hoje</a>
            <a class="btn btn-warning btn-sm" href="/imprimir" target="_blank">🖨️ Imprimir</a>
        </div>
    </div>
</nav>

<div class="container">
    {% block content %}{% endblock %}
</div>

</body>
</html>

     {% extends "base.html" %}
{% block content %}

<h3>Motos em serviço</h3>

<form method="POST" class="row g-2 mb-4">
    <div class="col-md-3">
        <input name="placa" class="form-control" placeholder="Placa" required>
    </div>
    <div class="col-md-3">
        <input name="modelo" class="form-control" placeholder="Modelo" required>
    </div>
    <div class="col-md-3">
        <input name="mecanico" class="form-control" placeholder="Mecânico" required>
    </div>
    <div class="col-md-3">
        <input name="servico" class="form-control" placeholder="Serviço" required>
    </div>
    <div class="col-md-12">
        <button class="btn btn-success w-100">➕ Dar entrada</button>
    </div>
</form>

<table class="table table-striped">
    <tr>
        <th>Placa</th>
        <th>Modelo</th>
        <th>Mecânico</th>
        <th>Serviço</th>
        <th>Entrada</th>
        <th>Ação</th>
    </tr>

    {% for m in motos %}
    <tr>
        <td>{{ m[1] }}</td>
        <td>{{ m[2] }}</td>
        <td>{{ m[3] }}</td>
        <td>{{ m[4] }}</td>
        <td>{{ m[5] }}</td>
        <td>
            <a class="btn btn-danger btn-sm" href="/saida/{{ m[0] }}">Finalizar</a>
        </td>
    </tr>
    {% endfor %}
</table>

{% endblock %}

    {% extends "base.html" %}
{% block content %}

<h3>Motos finalizadas hoje</h3>

<table class="table table-bordered">
    <tr>
        <th>Placa</th>
        <th>Modelo</th>
        <th>Mecânico</th>
        <th>Serviço</th>
        <th>Entrada</th>
        <th>Saída</th>
    </tr>

    {% for m in motos %}
    <tr>
        <td>{{ m[1] }}</td>
        <td>{{ m[2] }}</td>
        <td>{{ m[3] }}</td>
        <td>{{ m[4] }}</td>
        <td>{{ m[5] }}</td>
        <td>{{ m[6] }}</td>
    </tr>
    {% endfor %}
</table>

{% endblock %}

 <!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Relatório Diário</title>
</head>
<body onload="window.print()">

<h2>Relatório de Motos - Hoje</h2>

<table border="1" width="100%">
<tr>
    <th>Placa</th>
    <th>Modelo</th>
    <th>Mecânico</th>
    <th>Serviço</th>
    <th>Entrada</th>
    <th>Saída</th>
</tr>

{% for m in motos %}
<tr>
    <td>{{ m[1] }}</td>
    <td>{{ m[2] }}</td>
    <td>{{ m[3] }}</td>
    <td>{{ m[4] }}</td>
    <td>{{ m[5] }}</td>
    <td>{{ m[6] }}</td>
</tr>
{% endfor %}
</table>

</body>
</html>
